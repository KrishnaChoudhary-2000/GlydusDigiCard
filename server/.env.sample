# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration (Optional - app works with in-memory fallback)
MONGODB_URI=mongodb://localhost:27017/glydus-digital-cards

# Production Settings
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/glydus-digital-cards 